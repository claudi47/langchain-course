[project]
name = "langgaph-course"
version = "0.1.0"
description = ""
authors = [{ name = "Eden Marco", email = "emarco177@gmail.com" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain",
    "langgraph",
    "langchain-groq",
    "pydantic",
    "python-dotenv",
    "langchain-tavily",
]

[dependency-groups]
dev = [
    "black",
    "isort",
]

[tool.uv]
package = false
